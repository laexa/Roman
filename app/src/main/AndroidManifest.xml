<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="alexinc.roman">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Acitvity.SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Acitvity.LearnActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Acitvity.TestActivity"
            android:screenOrientation="portrait"/>

        <activity android:name=".Acitvity.MenuViewChoise"
            android:screenOrientation="portrait"/>

        <activity android:name=".Acitvity.MainActivity"
            android:theme="@style/themeForMainMenu"
            android:screenOrientation="portrait"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />

    </application>

</manifest>